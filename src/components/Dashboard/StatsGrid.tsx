import { TrendingUp, TrendingDown, DollarSign, ShoppingBag, Clock, BarChart2 } from "lucide-react";
import { Card, CardContent } from "@/src/components/ui/Card";
import { cn } from "@/src/lib/utils";
interface StatCardProps { title: string; value: string; change: string; trend: 'up' | 'down'; icon: any; }
function StatCard({ title, value, change, trend, icon: Icon }: StatCardProps) { return (<Card className="group relative overflow-hidden border-brand/10 transition-all duration-300 hover:border-brand/30 hover:shadow-brand/5 hover:shadow-2xl"><CardContent className="p-5 md:p-6"><div className="flex items-start justify-between"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-brand/10 text-brand transition-colors group-hover:bg-brand group-hover:text-white"><Icon className="h-5 w-5" /></div><div className={cn("flex items-center gap-1 rounded-full px-2 py-0.5 text-[10px] font-bold", trend === 'up' ? "bg-emerald-500/10 text-emerald-600 dark:text-emerald-400" : "bg-destructive/10 text-destructive")}>{trend === 'up' ? <TrendingUp className="h-3 w-3" /> : <TrendingDown className="h-3 w-3" />}{change}</div></div><div className="mt-4 space-y-1"><h3 className="text-xl md:text-2xl font-bold tracking-tight text-foreground">{value}</h3><p className="text-xs font-medium text-muted-foreground uppercase tracking-wider">{title}</p></div><div className="absolute -right-4 -top-4 h-24 w-24 rounded-full bg-brand/5 blur-3xl transition-opacity opacity-0 group-hover:opacity-100" /></CardContent></Card>); }
export function StatsGrid({ data }: { data?: any }) { const period = data?.period; return (<div className="grid grid-cols-2 gap-4 md:gap-6 lg:grid-cols-4"><StatCard title="Pedidos Pagos" value={data ? String(data.totalSales ?? data.paidOrders ?? 0) : '---'} change="Período" trend="up" icon={ShoppingBag} /><StatCard title={period === 'today' ? 'Pedidos Hoje' : 'Pedidos'} value={data ? String(period === 'today' ? (data.todayOrders ?? data.totalOrders ?? 0) : (data.totalOrders ?? data.todayOrders ?? 0)) : '---'} change="Período" trend="up" icon={Clock} /><StatCard title={period === 'today' ? 'Pendentes Hoje' : 'Pendentes'} value={data ? String(data.openOrders ?? 0) : '---'} change="Período" trend="down" icon={BarChart2} /><StatCard title={period === 'today' ? 'Faturamento Hoje' : 'Faturamento (Pago)'} value={data ? `R$ ${Number(data.revenue || 0).toLocaleString('pt-BR', { minimumFractionDigits: 2 })}` : '---'} change="Período" trend="up" icon={DollarSign} /></div>); }
